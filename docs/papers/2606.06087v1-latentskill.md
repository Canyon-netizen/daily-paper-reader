---
title: "LatentSkill: From In-Context Textual Skills to In-Weight Latent Skills for LLM Agents"
authors: "Aofan Yu, Chenyu Zhou, Tianyi Xu, Zihan Guo, Rong Shan, Zhihui Fu, Jun Wang, Weiwen Liu, Yong Yu, Weinan Zhang, Jianghao Lin"
date: 2026-06-04
generated_at: "2026-07-07 08:36:37 UTC"
pdf: "https://arxiv.org/pdf/2606.06087v1"
tags: ["query:2606"]
score: 7.0
evidence: "文本技能库在每一步注入 prompt 造成巨大上下文开销，且明文暴露专有流程；参数化方法如智能体微调则将技能不可逆地融入"
tldr: "针对 LLM 智能体反复将文本技能注入 prompt 导致上下文开销大、暴露明文的问题，提出 LatentSkill 框架，通过预训练超网络将文本技能转化为即插即用的 LoRA 适配器。在 ALFWorld 上 seen/unseen split 成功率分别提升 21.4 与 13.4 点，prefill token 减少 64.1%；Search-QA 精确匹配提升 3.0 点，技能 token 开销降低 72.2%。研究表明生成的技能 LoRA 在权重空间具有可分语义结构、可通过缩放系数 α 精确控制，并可通过参数空间算术组合。"
source: arxiv
selection_source: web_analyzer
motivation: "文本技能库在每一步注入 prompt 造成巨大上下文开销，且明文暴露专有流程；参数化方法如智能体微调则将技能不可逆地融入模型权重，丧失模块化更新与组合能力。"
method: "LatentSkill 通过预训练超网络一次前向将技能文本描述生成为对应 LoRA 适配器，推理时挂载到 backbone LLM，技能不再出现在 prompt 中，同时保留可加载、卸载、缩放与组合的模块化特性。"
result: "ALFWorld seen/unseen 成功率提升 21.4/13.4 点，prefill token 减少 64.1%；Search-QA EM 提升 3.0 点，技能 token 开销降低 72.2%；生成 LoRA 呈领域聚类结构，可由缩放系数 α 调控，支持参数空间算术组合。"
conclusion: "权重空间技能是一种高效、模块化、暴露更少的智能体扩展载体，既消除重复文本开销，又支持结构的检视、调节与复用。"
---

## TLDR
针对 LLM 智能体反复将文本技能注入 prompt 导致上下文开销大、暴露明文的问题，提出 LatentSkill 框架，通过预训练超网络将文本技能转化为即插即用的 LoRA 适配器。在 ALFWorld 上 seen/unseen split 成功率分别提升 21.4 与 13.4 点，prefill token 减少 64.1%；Search-QA 精确匹配提升 3.0 点，技能 token 开销降低 72.2%。研究表明生成的技能 LoRA 在权重空间具有可分语义结构、可通过缩放系数 α 精确控制，并可通过参数空间算术组合。

## Abstract
Agent systems increasingly use textual skills to encode reusable task procedures, but injecting these skills into the prompt at every step incurs substantial context overhead and exposes skill content as plaintext. We present LatentSkill, a framework that converts textual skills into plug-and-play LoRA adapters through a pretrained hypernetwork. LatentSkill stores skill knowledge in weight space rather than context space, removing per-step skill tokens while preserving modular loading, scaling, and composition. On ALFWorld and Search-QA, LatentSkill outperforms the corresponding in-context skill baseline while using substantially fewer prefill tokens: it improves ALFWorld success by 21.4 and 13.4 points on the seen and unseen splits with 64.1% fewer prefill tokens, and improves Search-QA exact match by 3.0 points with 72.2% lower skill-token overhead. Further analysis shows that generated skill LoRAs form a structured semantic geometry, can be precisely controlled via the LoRA scaling coefficient, and can be composed through parameter-space arithmetic when skill components are aligned. These findings suggest that weight-space skills provide an efficient, modular, and less exposed substrate for extending LLM agents.

## 动机
文本技能库在每一步注入 prompt 造成巨大上下文开销，且明文暴露专有流程；参数化方法如智能体微调则将技能不可逆地融入模型权重，丧失模块化更新与组合能力。

## 方法
LatentSkill 通过预训练超网络一次前向将技能文本描述生成为对应 LoRA 适配器，推理时挂载到 backbone LLM，技能不再出现在 prompt 中，同时保留可加载、卸载、缩放与组合的模块化特性。

## 结果
ALFWorld seen/unseen 成功率提升 21.4/13.4 点，prefill token 减少 64.1%；Search-QA EM 提升 3.0 点，技能 token 开销降低 72.2%；生成 LoRA 呈领域聚类结构，可由缩放系数 α 调控，支持参数空间算术组合。

## 结论
权重空间技能是一种高效、模块化、暴露更少的智能体扩展载体，既消除重复文本开销，又支持结构的检视、调节与复用。


