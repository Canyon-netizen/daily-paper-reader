{
  "step_id": "5.select",
  "checks": [
    { "kind": "no_error" },
    { "kind": "exit_code", "value": 0 },
    { "kind": "schema_valid", "field": "deep_dive", "required_keys": ["arxiv_id", "reason"] },
    { "kind": "schema_valid", "field": "quick_skim", "required_keys": ["arxiv_id", "reason"] }
  ],
  "rubric_enabled": false,
  "on_fail": "clamp"
}