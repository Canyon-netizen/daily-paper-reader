{
  "step_id": "0.enrich_config_queries",
  "checks": [
    { "kind": "no_error" },
    { "kind": "exit_code", "value": 0 },
    { "kind": "schema_valid", "field": "config", "required_keys": ["query_text", "prefixed_text"] }
  ],
  "rubric_enabled": false,
  "on_fail": "mark_needs_review"
}